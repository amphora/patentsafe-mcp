[project]
name = "patentsafe-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bs4>=0.0.2",
    "mcp[cli]>=1.2.1",
    "requests>=2.32.3",
]

[project.scripts]
patentsafe-mcp = "patentsafe_mcp:main"
